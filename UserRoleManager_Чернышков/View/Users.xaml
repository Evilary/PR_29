<Page x:Class="UserRoleManager_Чернышков.View.Users"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Users">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Users}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True" Width="70"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FIO}" Width="*"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="180"/>
                <DataGridComboBoxColumn Header="Роль"
                                        SelectedValueBinding="{Binding IdRole}"
                                        SelectedValuePath="Id"
                                        DisplayMemberPath="Name"
                                        Width="180"
                                        ItemsSource="{Binding DataContext.Roles, RelativeSource={RelativeSource AncestorType=Page}}"/>
                <DataGridTemplateColumn Header="Действия" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Сохранить"
                                        Margin="0,0,5,0"
                                        Command="{Binding DataContext.OnSaveUser, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="Удалить"
                                        Command="{Binding DataContext.OnDeleteUser, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="1"
                Content="Добавить пользователя"
                Width="180"
                Height="28"
                HorizontalAlignment="Right"
                Command="{Binding OnAddUser}"/>
    </Grid>
</Page>